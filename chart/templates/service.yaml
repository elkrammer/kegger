apiVersion: v1
kind: Service
metadata:
  name: {{ include "kegger.fullname" . }}
  labels:
    {{- include "kegger.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kegger.selectorLabels" . | nindent 4 }}
